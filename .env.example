# Estas variáveis de ambiente devem ser configuradas como Secrets no Replit
# Não inclua valores reais neste arquivo de exemplo

# Variável de ambiente para o domínio do site (URL completo com protocolo)
SITE_DOMAIN=https://seu-dominio.replit.dev

# API OpenWeatherMap (obtenha uma chave em https://openweathermap.org)
OPENWEATHERMAP_API_KEY=sua_chave_api_aqui